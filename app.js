
const KEY='bidking-web-v1';
const Q={白色:{c:'#d6d7dc',d:500,k:'quality-white'},绿色:{c:'#3fbf60',d:1000,k:'quality-green'},蓝色:{c:'#2f7dff',d:1500,k:'quality-blue'},紫色:{c:'#8f52ff',d:2000,k:'quality-purple'},金色:{c:'#e7b82f',d:3000,k:'quality-gold'},红色:{c:'#e14848',d:5000,k:'quality-red'}};
const MAPS=[
{id:'express',name:'快递站',entry:0,q:{白色:40,绿色:28,蓝色:18,紫色:9,金色:4,红色:1},subs:[['豪宅快递站',{古董文物:120,武器:70,医疗器材:80,交通能源:90,娱乐数码:140,珠宝矿藏:180}],['学者区快递站',{古董文物:200,武器:80,医疗器材:120,交通能源:90,娱乐数码:80,珠宝矿藏:90}],['医院快递站',{古董文物:80,武器:70,医疗器材:210,交通能源:110,娱乐数码:90,珠宝矿藏:70}]]},
{id:'waste',name:'废弃场景',entry:100000,q:{白色:34,绿色:28,蓝色:20,紫色:11,金色:5,红色:2},subs:[['废弃医院',{古董文物:110,武器:90,医疗器材:220,交通能源:100,娱乐数码:90,珠宝矿藏:80}],['废弃军械库',{古董文物:90,武器:230,医疗器材:75,交通能源:120,娱乐数码:75,珠宝矿藏:80}],['废弃车库',{古董文物:90,武器:110,医疗器材:90,交通能源:220,娱乐数码:90,珠宝矿藏:85}]]},
{id:'container',name:'集装箱',entry:500000,q:{白色:30,绿色:26,蓝色:22,紫色:13,金色:6,红色:3},subs:[['私人收藏箱',{古董文物:180,武器:90,医疗器材:100,交通能源:100,娱乐数码:100,珠宝矿藏:160}],['科考设备箱',{古董文物:100,武器:80,医疗器材:130,交通能源:150,娱乐数码:130,珠宝矿藏:80}],['电子走私箱',{古董文物:80,武器:90,医疗器材:90,交通能源:110,娱乐数码:220,珠宝矿藏:110}]]},
{id:'villa',name:'别墅',entry:1000000,q:{白色:24,绿色:24,蓝色:24,紫色:16,金色:8,红色:4},subs:[['学者的别墅',{古董文物:230,武器:80,医疗器材:120,交通能源:70,娱乐数码:75,珠宝矿藏:90}],['收藏家的别墅',{古董文物:180,武器:90,医疗器材:100,交通能源:90,娱乐数码:100,珠宝矿藏:170}],['发明家的别墅',{古董文物:90,武器:90,医疗器材:100,交通能源:180,娱乐数码:160,珠宝矿藏:80}]]},
{id:'cruise',name:'游轮',entry:5000000,q:{白色:18,绿色:22,蓝色:25,紫色:18,金色:11,红色:6},subs:[['贵宾收藏舱',{古董文物:190,武器:90,医疗器材:100,交通能源:90,娱乐数码:110,珠宝矿藏:200}],['黑市交易舱',{古董文物:110,武器:180,医疗器材:90,交通能源:100,娱乐数码:130,珠宝矿藏:160}],['机械动力舱',{古董文物:80,武器:110,医疗器材:90,交通能源:240,娱乐数码:90,珠宝矿藏:90}]]}
];
const ROLES=[
{id:'r1',name:'古董文物鉴定师',type:'cat',cat:'古董文物',txt:'1-4回合逐步揭示文物'},
{id:'r2',name:'武器鉴定师',type:'cat',cat:'武器',txt:'1-4回合逐步揭示武器'},
{id:'r3',name:'医疗器材鉴定师',type:'cat',cat:'医疗器材',txt:'1-4回合逐步揭示医疗'},
{id:'r4',name:'交通能源鉴定师',type:'cat',cat:'交通能源',txt:'1-4回合逐步揭示交通能源'},
{id:'r5',name:'娱乐数码鉴定师',type:'cat',cat:'娱乐数码',txt:'1-4回合逐步揭示数码'},
{id:'r6',name:'珠宝矿藏鉴定师',type:'cat',cat:'珠宝矿藏',txt:'1-4回合逐步揭示珠宝'},
{id:'r8',name:'稀有度监察员',type:'global',txt:'第1回合看紫金红总数'},
{id:'r9',name:'首席估值员',type:'global',txt:'第1回合看最高价品质'},
{id:'r10',name:'品质抽样师',type:'global',txt:'持续抽样揭示品质'},
{id:'r12',name:'轮廓抽样师',type:'global',txt:'持续抽样揭示轮廓'}
];
const TOOLS=[
{id:'t1',name:'最高品相探针',price:3000,desc:'看最高价品质',fx:'hq'},
{id:'t2',name:'稀有计数器',price:2400,desc:'看紫金红总数',fx:'rare'},
{id:'t3',name:'红货监听器',price:1800,desc:'看是否有红货',fx:'red'},
{id:'t4',name:'最大轮廓扫描仪',price:2800,desc:'看最大轮廓',fx:'largest'},
{id:'t5',name:'最高价全息镜',price:5000,desc:'看最高价完整信息',fx:'full'},
{id:'t6',name:'品质采样器',price:1600,desc:'随机看2件品质',fx:'sample'}
];
const DOCS=[['地图设计','./docs/bidking_map_design_v1.md'],['角色设计','./docs/bidking_roles_v1.md'],['物品设计','./docs/bidking_items_design_v2.md'],['道具设计','./docs/bidking_tools_design_v1.md'],['竞拍流转','./docs/bidking_auction_flow_v1.md'],['OpenSpec Proposal','./openspec/changes/add-web-auction-prototype/proposal.md']];
const E={m:document.getElementById('moneyValue'),w:document.getElementById('warehouseCount'),t:document.getElementById('toolCount'),nav:document.getElementById('mainNav'),home:document.getElementById('homeView'),setup:document.getElementById('auctionSetupView'),auction:document.getElementById('auctionView'),settle:document.getElementById('settlementView'),shop:document.getElementById('shopView'),store:document.getElementById('warehouseView'),docs:document.getElementById('docsView')};
const DEF={money:1200000,warehouse:[],tools:{t1:1,t2:1,t3:0,t4:1,t5:0,t6:0},history:[]};
const A={data:null,state:load(),view:'home',setup:{map:'express',role:'r1',tools:[]},session:null,timers:[]};
init();
async function init(){A.data=await fetch('./data/bidking_items_seed_v1.json').then(r=>r.json());bind();render();}
function load(){try{const s=JSON.parse(localStorage.getItem(KEY)||'null');return s?{...DEF,...s,tools:{...DEF.tools,...(s.tools||{})},warehouse:s.warehouse||[],history:s.history||[]}:structuredClone(DEF);}catch{return structuredClone(DEF);}}
function save(){localStorage.setItem(KEY,JSON.stringify(A.state));}
function bind(){E.nav.onclick=e=>{const b=e.target.closest('[data-view]');if(b)setView(b.dataset.view);};}
function setView(v){if((v==='auction'||v==='settlement')&&!A.session)v='auction-setup';A.view=v;document.querySelectorAll('.nav-btn').forEach(b=>b.classList.toggle('is-active',b.dataset.view===v));[E.home,E.setup,E.auction,E.settle,E.shop,E.store,E.docs].forEach(x=>x.classList.remove('is-active'));({home:E.home,'auction-setup':E.setup,auction:E.auction,settlement:E.settle,shop:E.shop,warehouse:E.store,docs:E.docs})[v].classList.add('is-active');}
function mapById(id){return MAPS.find(x=>x.id===id)||MAPS[0];}
function roleById(id){return ROLES.find(x=>x.id===id)||ROLES[0];}
function toolById(id){return TOOLS.find(x=>x.id===id);}
function fm(v){return `${Math.round(v).toLocaleString('zh-CN')} 元`;}
function fs(v){return `${v>=0?'+':'-'}${Math.abs(Math.round(v)).toLocaleString('zh-CN')} 元`;}
function pick(arr){return arr[Math.floor(Math.random()*arr.length)];}
function wp(obj){const es=Object.entries(obj);let t=es.reduce((s,[,w])=>s+w,0),r=Math.random()*t;for(const [k,w]of es){r-=w;if(r<=0)return k}return es.at(-1)[0]}
function gq(items){return items.reduce((a,i)=>(a[i.quality]=(a[i.quality]||0)+1,a),{});} 
function gc(items){return items.reduce((a,i)=>(a[i.category]=(a[i.category]||0)+1,a),{});} 
function highest(items){return [...items].sort((a,b)=>b.recyclePrice-a.recyclePrice)[0];}
function largest(items){return [...items].sort((a,b)=>b.cells-a.cells||b.recyclePrice-a.recyclePrice)[0];}
function sample(items,n){return [...items].sort(()=>Math.random()-.5).slice(0,n);}
function kmerge(k,id,data){k[id]={...(k[id]||{}),...data};}
function shape(w,h,q,outlineOnly=false,colorOnly=false){const color=q?Q[q].c:'#fff';let s=`<div class="reveal-shape" style="grid-template-columns:repeat(${w},18px)">`;for(let i=0;i<w*h;i++)s+=`<span class="cell ${outlineOnly?'unknown':''}" style="${outlineOnly?'':`background:${colorOnly?'#fff':color};${colorOnly?'border:1px dashed rgba(0,0,0,.35);':''}`}"></span>`;return s+'</div>';}
function render(){E.m.textContent=fm(A.state.money);E.w.textContent=A.state.warehouse.length;E.t.textContent=Object.values(A.state.tools).reduce((s,n)=>s+n,0);renderHome();renderSetup();renderShop();renderStore();renderDocs();if(A.session){renderAuction();if(A.view==='settlement')renderSettlement();}setView(A.view);} 
function renderHome(){const h=A.state.history.slice(-5).reverse().map(x=>`<div class="log-item"><strong>${x.result}</strong><br><span class="muted">${x.map} / ${x.sub} / 成交价 ${fm(x.bid)} / 盈亏 ${fs(x.profit)}</span></div>`).join('')||'<div class="log-item">还没有竞拍记录。</div>';E.home.innerHTML=`<div class="home-grid"><div class="panel"><h2 class="section-title">原型状态</h2><p class="muted">当前为浏览器本地原型：地图门槛、角色、道具、竞拍、结算、仓库、商店全部在前端跑通。</p><div class="badge-row"><span class="badge">5回合竞拍</span><span class="badge">AI对手</span><span class="badge">本地存档</span><span class="badge">固定回收价</span></div><div class="surface-card" style="margin-top:16px;"><h3>快速开始</h3><p class="muted">资金不足时无法进入高阶地图。去商店补道具后，再到竞拍页带入。</p><div class="inline-actions" style="margin-top:12px;"><button class="primary-btn" id="goA">进入竞拍</button><button class="secondary-btn" id="goS">去商店</button></div></div></div><div class="panel"><h2 class="section-title">最近结算</h2><div class="summary-list">${h}</div></div></div>`;document.getElementById('goA').onclick=()=>setView('auction-setup');document.getElementById('goS').onclick=()=>setView('shop');}
function renderSetup(){const m=mapById(A.setup.map),r=roleById(A.setup.role);E.setup.innerHTML=`<div class="setup-grid"><div class="panel"><h2 class="section-title">地图选择</h2><div class="map-grid">${MAPS.map(x=>`<article class="map-card ${x.id===A.setup.map?'is-selected':''}"><h3>${x.name}</h3><p class="meta-line">门槛：${x.entry?fm(x.entry):'不限'}</p><p class="meta-line">子场景：${x.subs.length}</p><div class="badge-row"><span class="badge">${A.state.money>=x.entry?'可进入':'资金不足'}</span></div><button class="secondary-btn" data-map="${x.id}" style="margin-top:12px;" ${A.state.money<x.entry?'disabled':''}>选择</button></article>`).join('')}</div></div><div class="split-grid"><div class="panel"><h2 class="section-title">角色</h2><div class="role-grid">${ROLES.map(x=>`<article class="role-card ${x.id===A.setup.role?'is-selected':''}"><h3>${x.name}</h3><p class="meta-line">${x.txt}</p><button class="secondary-btn" data-role="${x.id}" style="margin-top:12px;">选择</button></article>`).join('')}</div></div><div class="panel"><h2 class="section-title">道具带入</h2><p class="muted">最多 5 种不同道具，同种单局只能带 1 个。</p><div class="tool-grid">${TOOLS.map(x=>{const n=A.state.tools[x.id]||0,s=A.setup.tools.includes(x.id);return `<article class="tool-card ${s?'is-selected':''}"><h3>${x.name}</h3><p class="meta-line">${x.desc}</p><div class="badge-row"><span class="badge">库存 ${n}</span><span class="badge">售价 ${fm(x.price)}</span></div><button class="secondary-btn" data-tool="${x.id}" style="margin-top:12px;" ${n<=0&&!s?'disabled':''}>${s?'取消带入':'带入本局'}</button></article>`}).join('')}</div></div></div><div class="panel"><h2 class="section-title">准备完成</h2><div class="summary-list"><div class="log-item">地图：<strong>${m.name}</strong></div><div class="log-item">角色：<strong>${r.name}</strong></div><div class="log-item">带入道具：<strong>${A.setup.tools.length}</strong> / 5</div></div><div class="inline-actions" style="margin-top:14px;"><button class="primary-btn" id="startBtn" ${A.state.money<m.entry?'disabled':''}>开始竞拍</button></div></div></div>`;
E.setup.querySelectorAll('[data-map]').forEach(b=>b.onclick=()=>{A.setup.map=b.dataset.map;renderSetup();});
E.setup.querySelectorAll('[data-role]').forEach(b=>b.onclick=()=>{A.setup.role=b.dataset.role;renderSetup();});
E.setup.querySelectorAll('[data-tool]').forEach(b=>b.onclick=()=>{const id=b.dataset.tool;A.setup.tools=A.setup.tools.includes(id)?A.setup.tools.filter(x=>x!==id):(A.setup.tools.length<5?[...A.setup.tools,id]:A.setup.tools);renderSetup();});
document.getElementById('startBtn').onclick=startAuction;}
function renderShop(){E.shop.innerHTML=`<div class="panel"><h2 class="section-title">商店</h2><div class="shop-grid">${TOOLS.map(x=>`<article class="shop-card"><h3>${x.name}</h3><p class="meta-line">${x.desc}</p><div class="badge-row"><span class="badge">价格 ${fm(x.price)}</span><span class="badge">库存 ${A.state.tools[x.id]||0}</span></div><button class="primary-btn" data-buy="${x.id}" style="margin-top:12px;" ${A.state.money<x.price?'disabled':''}>购买</button></article>`).join('')}</div></div>`;E.shop.querySelectorAll('[data-buy]').forEach(b=>b.onclick=()=>{const t=toolById(b.dataset.buy);if(A.state.money<t.price)return;A.state.money-=t.price;A.state.tools[t.id]=(A.state.tools[t.id]||0)+1;save();render();});}
function renderStore(){const qs=gq(A.state.warehouse);E.store.innerHTML=`<div class="panel"><h2 class="section-title">仓库</h2><div class="badge-row">${Object.keys(Q).map(k=>`<span class="quality-chip ${Q[k].k}">${k} ${qs[k]||0}</span>`).join('')}</div><div class="warehouse-grid" style="margin-top:18px;">${A.state.warehouse.length?A.state.warehouse.map((x,i)=>`<article class="warehouse-card"><h3>${x.name}</h3><p class="meta-line">${x.category} / ${x.quality} / ${x.width}x${x.height}</p><div class="badge-row"><span class="quality-chip ${Q[x.quality].k}">${x.quality}</span><span class="badge">回收价 ${fm(x.recyclePrice)}</span></div>${shape(x.width,x.height,x.quality)}<button class="secondary-btn" data-sell="${i}" style="margin-top:12px;">直接出售</button></article>`).join(''):'<div class="log-item">仓库还是空的。</div>'}</div></div>`;E.store.querySelectorAll('[data-sell]').forEach(b=>b.onclick=()=>{const i=+b.dataset.sell,x=A.state.warehouse[i];A.state.money+=x.recyclePrice;A.state.warehouse.splice(i,1);save();render();});}
function renderDocs(){E.docs.innerHTML=`<div class="panel"><h2 class="section-title">资料与规范</h2><div class="docs-grid">${DOCS.map(x=>`<article class="doc-card"><h3>${x[0]}</h3><a class="primary-btn" style="display:inline-block;text-decoration:none;margin-top:12px;" href="${x[1]}" target="_blank">打开</a></article>`).join('')}</div></div>`;}
function startAuction(){const m=mapById(A.setup.map);if(A.state.money<m.entry)return alert('资金不足');const sub=pick(m.subs),role=roleById(A.setup.role),box=makeBox(m,sub);A.session={map:m,sub:{name:sub[0],w:sub[1]},round:1,box,player:{id:'me',name:'你',role,tools:A.setup.tools.map(id=>({...toolById(id),used:false})),bid:0,used:false,done:false,logs:[],know:{}},ais:[1,2,3].map(i=>mkAI(i)),pub:[],infos:[],winner:null,bid:0,second:0};roundReveal();renderAuction();setView('auction');}
function mkAI(i){return{id:'ai'+i,name:'AI-'+i,role:pick(ROLES),tools:[...TOOLS].sort(()=>Math.random()-.5).slice(0,3).map(x=>({...x,used:false})),bid:0,used:false,done:false,logs:[],know:{}};}
function makeBox(m,sub){const pool=A.data.items;const target=m.entry>=1000000?rand(90,170):rand(60,130);let cells=0,items=[],used=new Set(),tries=0;while(cells<target&&items.length<20&&tries<220){tries++;const q=wp(m.q),c=wp(sub[1]);const exact=pool.filter(x=>x.quality===q&&x.category===c&&!used.has(x.id));const any=pool.filter(x=>x.category===c&&!used.has(x.id));const it=pick(exact.length?exact:any);if(!it)break;if(cells+it.cells>200&&items.length>=8)continue;used.add(it.id);items.push({...it,revealId:it.id});cells+=it.cells;}return{items,total:items.reduce((s,x)=>s+x.recyclePrice,0),cells:items.reduce((s,x)=>s+x.cells,0)};}
function roundReveal(){const s=A.session,it=s.box.items,qq=gq(it),dom=Object.entries(gc(it)).sort((a,b)=>b[1]-a[1])[0];const pub=[`本盒总藏品数为 ${it.length} 件。`,`本盒总占格数为 ${s.box.cells} 格。`,`本盒紫色及以上藏品共有 ${(qq.紫色||0)+(qq.金色||0)+(qq.红色||0)} 件。`,`本盒最大轮廓占格为 ${Math.max(...it.map(x=>x.cells))}。`,`本盒高发类别为 ${dom[0]}（${dom[1]} 件）。`][Math.min(s.round-1,4)];s.infos.push(pub);s.pub.push(`[公共] 第 ${s.round} 回合：${pub}`);roleReveal(s.player);}
function roleReveal(p){const s=A.session,it=s.box.items;if(p.role.type==='cat'){const ms=it.filter(x=>x.category===p.role.cat);if(s.round===1)p.logs.push(`[角色] ${p.role.cat} 共 ${ms.length} 件。`);if(s.round===2){ms.forEach(x=>kmerge(p.know,x.revealId,{width:x.width,height:x.height}));p.logs.push(`[角色] 已揭示 ${ms.length} 件 ${p.role.cat} 轮廓。`)}if(s.round===3){ms.forEach(x=>kmerge(p.know,x.revealId,{quality:x.quality}));p.logs.push(`[角色] 已揭示 ${ms.length} 件 ${p.role.cat} 品质。`)}if(s.round===4){ms.forEach(x=>kmerge(p.know,x.revealId,{quality:x.quality,width:x.width,height:x.height}));p.logs.push(`[角色] 已完全揭示 ${ms.length} 件 ${p.role.cat}。`)}return;}
if(p.role.id==='r8'&&s.round===1)p.logs.push(`[角色] 紫金红总数为 ${it.filter(x=>['紫色','金色','红色'].includes(x.quality)).length}。`);
if(p.role.id==='r9'&&s.round===1){const h=highest(it);kmerge(p.know,h.revealId,{quality:h.quality});p.logs.push(`[角色] 最高价品质为 ${h.quality}。`);}
if(p.role.id==='r10'){const arr=sample(it,s.round===1?5:2);arr.forEach(x=>kmerge(p.know,x.revealId,{quality:x.quality}));p.logs.push(`[角色] 本回合抽样揭示 ${arr.length} 件品质。`);}
if(p.role.id==='r12'){const arr=s.round<5?sample(it,s.round===1?5:2):it;arr.forEach(x=>kmerge(p.know,x.revealId,{width:x.width,height:x.height}));p.logs.push(`[角色] 已揭示 ${arr.length} 件轮廓。`);}}
function renderAuction(){const s=A.session,p=s.player,sec=secondBid(),thr=thrOf(s.round),lead=leader();E.auction.innerHTML=`<div class="auction-layout"><div class="info-column"><div class="info-block"><h3>公共信息</h3><div class="log-list">${s.infos.map((x,i)=>`<div class="log-item">第 ${i+1} 回合：${x}</div>`).join('')}</div></div><div class="info-block"><h3>公开日志</h3><div class="log-list">${s.pub.slice().reverse().map(x=>`<div class="log-item">${x}</div>`).join('')}</div></div><div class="info-block"><h3>你的私人情报</h3><div class="log-list">${p.logs.slice().reverse().map(x=>`<div class="log-item">${x}</div>`).join('')}</div></div></div><div class="board-column"><div class="panel"><div class="auction-header"><div><p class="eyebrow">${s.map.name} / ${s.sub.name}</p><h2 class="section-title">第 ${s.round} 回合竞拍中</h2></div><div class="hero-kpis"><div class="kpi">阈值：<strong>${thr.label}</strong></div><div class="kpi">领先者：<strong>${lead?lead.name:'暂无'}</strong></div><div class="kpi">第二名：<strong>${fm(sec)}</strong></div></div></div><p class="muted">盲盒区展示的是你当前视角下已知的信息。</p><div class="knowledge-board">${Object.entries(p.know).length?Object.entries(p.know).map(([id,info])=>{const it=s.box.items.find(x=>x.revealId===id),w=info.width||1,h=info.height||1,q=info.quality||null;return `<article class="item-card"><h4>${it?it.name:'未知目标'}</h4><p class="meta-line">${q||'品质未知'} / ${info.width&&info.height?`${info.width}x${info.height}`:'轮廓未知'}</p>${shape(w,h,q,!info.width||!info.height,!q)}</article>`}).join(''):'<div class="empty-board">情报尚未揭示</div>'}</div></div><div class="panel actions-panel"><div class="bid-box"><h3>本回合出价</h3><p class="muted">只能提交一次。${sec>0?`当前拍下参考价：${fm(Math.ceil(sec*thr.mul))}`:'当前暂无参考第二价。'}</p><input id="bidInput" type="number" min="0" step="100" value="${p.bid||''}" ${p.done?'disabled':''}><div class="inline-actions"><button class="pill-btn" data-m="1.2">×1.2</button><button class="pill-btn" data-m="1.4">×1.4</button><button class="pill-btn" data-m="1.6">×1.6</button><button class="primary-btn" id="bidBtn" ${p.done?'disabled':''}>确认出价</button><button class="secondary-btn" id="skipBtn" ${p.done?'disabled':''}>本回合放弃</button></div></div><div><h3>道具</h3><div class="tool-list">${p.tools.map(t=>`<article class="tool-card ${t.used?'used':''}"><h3>${t.name}</h3><p class="meta-line">${t.desc}</p><button class="tool-use-btn" data-tool-use="${t.id}" ${t.used||p.used||p.done?'disabled':''}>${t.used?'已消耗':'本回合使用'}</button></article>`).join('')}</div></div></div></div><div class="side-column"><div class="info-block"><h3>竞拍者</h3><div class="bidder-list">${[p,...s.ais].map(x=>`<article class="bidder-card ${lead&&lead.id===x.id?'leading':''}"><h3>${x.name}</h3><p class="meta-line">${x.role.name}</p><div class="badge-row"><span class="badge">当前出价 ${fm(x.bid)}</span><span class="badge">${x.done?'已出价':'未出价'}</span><span class="badge">${x.used?'已用道具':'未用道具'}</span></div>${x.lastTool?`<p class="small-line" style="margin-top:10px;">本回合道具：${x.lastTool}</p>`:''}</article>`).join('')}</div></div></div></div>`;
E.auction.querySelectorAll('[data-m]').forEach(b=>b.onclick=()=>{document.getElementById('bidInput').value=Math.ceil((sec||50000)*+b.dataset.m)});
E.auction.querySelectorAll('[data-tool-use]').forEach(b=>b.onclick=()=>useTool(b.dataset.toolUse));
document.getElementById('bidBtn').onclick=submitBid;document.getElementById('skipBtn').onclick=()=>{document.getElementById('bidInput').value=0;submitBid();};}
function useTool(id){const s=A.session,p=s.player,t=p.tools.find(x=>x.id===id);if(!t||t.used||p.used)return;const it=s.box.items;if(t.fx==='hq'){const h=highest(it);kmerge(p.know,h.revealId,{quality:h.quality});p.logs.push(`[道具] ${t.name}：最高价品质为 ${h.quality}。`);} if(t.fx==='rare'){p.logs.push(`[道具] ${t.name}：紫金红总数为 ${it.filter(x=>['紫色','金色','红色'].includes(x.quality)).length}。`);} if(t.fx==='red'){p.logs.push(`[道具] ${t.name}：${it.some(x=>x.quality==='红色')?'检测到红色藏品。':'未检测到红色藏品。'}`);} if(t.fx==='largest'){const h=largest(it);kmerge(p.know,h.revealId,{width:h.width,height:h.height});p.logs.push(`[道具] ${t.name}：最大轮廓为 ${h.width}x${h.height}。`);} if(t.fx==='full'){const h=highest(it);kmerge(p.know,h.revealId,{quality:h.quality,width:h.width,height:h.height});p.logs.push(`[道具] ${t.name}：已完全揭示最高价藏品。`);} if(t.fx==='sample'){const arr=sample(it,2);arr.forEach(x=>kmerge(p.know,x.revealId,{quality:x.quality}));p.logs.push(`[道具] ${t.name}：随机揭示 ${arr.length} 件品质。`);} t.used=true;p.used=true;p.lastTool=t.name;s.pub.push(`[公开行为] 你 使用了【${t.name}】`);renderAuction();}
function submitBid(){const s=A.session,p=s.player;p.bid=Math.max(0,+document.getElementById('bidInput').value||0);p.done=true;aiTurn();resolveRound();}
function aiTurn(){const s=A.session,top=Math.max(...[s.player,...s.ais].map(x=>x.bid),0);s.ais.forEach(ai=>{ai.used=false;ai.done=false;ai.lastTool='';const t=ai.tools.find(x=>!x.used&&['t5','t1','t2','t4'].includes(x.id));if(t&&s.round<=3){aiUse(ai,t);}ai.bid=aiBid(ai,top);ai.done=true;});}
function aiUse(ai,t){const it=A.session.box.items;if(t.fx==='hq'){const h=highest(it);kmerge(ai.know,h.revealId,{quality:h.quality});} if(t.fx==='largest'){const h=largest(it);kmerge(ai.know,h.revealId,{width:h.width,height:h.height});} if(t.fx==='full'){const h=highest(it);kmerge(ai.know,h.revealId,{quality:h.quality,width:h.width,height:h.height});} t.used=true;ai.used=true;ai.lastTool=t.name;A.session.pub.push(`[公开行为] ${ai.name} 使用了【${t.name}】`);}
function aiBid(ai,top){const s=A.session,it=s.box.items,dom=Object.entries(gc(it)).sort((a,b)=>b[1]-a[1])[0][0];let conf=.38+s.round*.11;if(ai.role.cat===dom)conf+=.16;if(ai.used)conf+=.08;if(ai.role.id==='r8'||ai.role.id==='r9')conf+=.05;conf=Math.min(conf,.88);const will=Math.floor(s.box.total*conf);const d=top>0?Math.max(Math.ceil(top*(1.04+Math.random()*.08)),Math.floor(will*(.85+Math.random()*.2))):Math.floor(will*(.3+Math.random()*.22));return d<=top?0:d;}
function leader(){return [...A.session.ais,A.session.player].sort((a,b)=>b.bid-a.bid)[0]||null;}
function secondBid(){const xs=[A.session.player,...A.session.ais].map(x=>x.bid).sort((a,b)=>b-a);return xs[1]||0;}
function thrOf(r){return {1:{mul:2,label:'第一名 >= 第二名 × 2.0'},2:{mul:1.6,label:'第一名 >= 第二名 × 1.6'},3:{mul:1.4,label:'第一名 >= 第二名 × 1.4'},4:{mul:1.2,label:'第一名 >= 第二名 × 1.2'},5:{mul:1,label:'价高者直接成交'}}[r];}
function resolveRound(){const s=A.session,b=[s.player,...s.ais].map(x=>({id:x.id,name:x.name,bid:x.bid})).sort((a,b)=>b.bid-a.bid),f=b[0],sec=b[1]||{bid:0},t=thrOf(s.round),ok=s.round===5?f.bid>0:f.bid>=Math.ceil(sec.bid*t.mul);s.bid=f.bid;s.second=sec.bid;if(ok)return finish(f.id,f.bid);s.pub.push(`[回合结论] 第 ${s.round} 回合未拍下，领先价 ${fm(f.bid)}，第二名 ${fm(sec.bid)}。`);s.round++;[s.player,...s.ais].forEach(x=>{x.used=false;x.done=false;x.lastTool='';});roundReveal();renderAuction();}
function finish(id,bid){const s=A.session;s.winner=id;s.bid=bid;s.pub.push(`[拍下] ${([s.player,...s.ais].find(x=>x.id===id)||{name:'未知'}).name} 以 ${fm(bid)} 拍下盲盒。`);beginSettlement();}
function beginSettlement(){clearTimers();const s=A.session;s.settle={revealed:[],value:0,colors:[]};s.box.items.forEach(it=>A.timers.push(setTimeout(()=>{if(!s.settle.revealed.includes(it.revealId)){s.settle.revealed.push(it.revealId);s.settle.value+=it.recyclePrice;renderSettlement();}},Q[it.quality].d)));renderSettlement();setView('settlement');}
function clearTimers(){A.timers.forEach(clearTimeout);A.timers=[];}
function renderSettlement(){const s=A.session,st=s.settle,win=s.winner==='me',seen=new Set(st.revealed),profit=st.value-s.bid;E.settle.innerHTML=`<div class="settlement-grid"><div class="panel"><h2 class="section-title">结算揭示</h2><p class="muted">赢家：${win?'你':'AI 对手'}。先展示轮廓，再按品质延时揭示完整信息。</p><div class="warehouse-grid">${s.box.items.map(it=>`<article class="item-card"><h4>${seen.has(it.revealId)?it.name:'未完全揭示'}</h4><p class="meta-line">${seen.has(it.revealId)?`${it.category} / ${it.quality} / 回收价 ${fm(it.recyclePrice)}`:`${it.width}x${it.height} 轮廓已显示`}</p>${shape(it.width,it.height,seen.has(it.revealId)?it.quality:null,false,!seen.has(it.revealId))}</article>`).join('')}</div></div><div class="panel"><h2 class="section-title">结算面板</h2><div class="summary-list"><div class="log-item">成交价：<strong>${fm(s.bid)}</strong></div><div class="log-item">当前已揭示回收总价：<strong>${fm(st.value)}</strong></div><div class="log-item">当前盈亏：<strong class="${profit>=0?'money-positive':'money-negative'}">${fs(profit)}</strong></div></div>${win?`<div class="surface-card" style="margin-top:16px;"><h3>按颜色立即卖出</h3><div class="checkbox-row">${Object.keys(Q).map(k=>`<label><input type="checkbox" data-color="${k}" ${st.colors.includes(k)?'checked':''}> ${k}</label>`).join('')}</div><p class="footer-note">勾选什么颜色，就卖出什么颜色；未勾选的会进入仓库。</p></div>`:`<div class="surface-card" style="margin-top:16px;"><h3>你未拍下本盒</h3><p class="muted">本次仅观看完整揭示，不会获得藏品。</p></div>`}<div class="inline-actions" style="margin-top:18px;"><button class="primary-btn" id="doneSettle">完成结算</button><button class="secondary-btn" id="backHome">返回大厅</button></div></div></div>`;
E.settle.querySelectorAll('[data-color]').forEach(x=>x.onchange=()=>{st.colors=x.checked?[...st.colors,x.dataset.color]:st.colors.filter(v=>v!==x.dataset.color);});
document.getElementById('doneSettle').onclick=doneSettlement;document.getElementById('backHome').onclick=()=>{A.session=null;clearTimers();render();setView('home');};}
function doneSettlement(){const s=A.session;clearTimers();let profit=0;if(s.winner==='me'){const sold=new Set(s.settle.colors);let soldV=0,keep=[];s.box.items.forEach(it=>sold.has(it.quality)?soldV+=it.recyclePrice:keep.push(it));A.state.money=A.state.money-s.bid+soldV;A.state.warehouse=[...A.state.warehouse,...keep];profit=soldV+keep.reduce((n,x)=>n+x.recyclePrice,0)-s.bid;}A.state.history.push({map:s.map.name,sub:s.sub.name,bid:s.bid,profit,result:s.winner==='me'?'你拍下了盲盒':'AI 拍下了盲盒'});save();A.session=null;render();setView(s.winner==='me'?'warehouse':'home');}
function rand(a,b){return Math.floor(Math.random()*(b-a+1))+a;}
